service: projects-service
provider:
  name: ssm

secret:
  path: /${stage}/projects/secret
  required:
    DATABASE_NAME: 'Database name for prisma server'
    DATABASE_PASSWORD: 'Database password for prisma server'
